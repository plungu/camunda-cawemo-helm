{{- if .Values.garufa.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "camunda-cawemo.fullname" . }}-{{ .Values.garufa.containerName }}
  labels:
    {{- include "camunda-cawemo.labels" . | nindent 4 }}
    {{- include "camunda-cawemo-garufa.labels" . | nindent 4 }}

spec:
  type: {{ .Values.garufa.service.type }}
  ports:
    - port: {{ .Values.garufa.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "camunda-cawemo-garufa.selectorLabels" . | nindent 4 }}
{{- end }}